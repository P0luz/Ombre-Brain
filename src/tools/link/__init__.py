"""
========================================
tools/link/__init__.py — link 补线 / unlink 断线工具入口
========================================

link 是「补线」：手动声明一条跨时间的边（3.5.0）。
unlink 是「断线」：连错了就软删掉（3.8.0）。
时间线给「顺序」（然后呢），补线给「叙事」（所以呢）——跨越时间的经历连续。

对外暴露：link / unlink
========================================
"""

from .core import link, unlink  # noqa: F401
